# Implementation Notes

## 2026-05-24

- Initialized this repository as Inventory Validation Site.
- Rewrote the previous placeholder README that described legacy compatibility
  routes.
- No legacy route is preserved.
- `pnpm-lock.yaml` is generated by `pnpm install`; it is not hand-written.
- WebAgentFlow v0.1 product-test-site validation intent is migrated into docs,
  while old page implementation details are excluded.

## 2026-05-29

- Added a root catalog page modeled after the external fixture-site list layout,
  but renamed for this repository as `WebAgentFlow Validation Site`.
- Added a small Inventory API under `/api/inventory` for list, create, update,
  and CSV export.
- The API is a visible product backend for the Inventory page, not a hidden
  fixture reset/seed/control API.
- `pnpm dev` now starts both the frontend on `5177` and the Inventory API on
  `8003`.
